  <section class="jumbotron text-center">
 <div class="container">
          <h1 class="jumbotron-heading">Notre joli site de Potins</h1>
          <h2 class="lead text-muted">welcome <%= @name %> ! Ici c'est notre super site de potins, il est chouette, non ?</h2>
          <p>
            <a href="<%= new_gossip_path %>" class="btn btn-success my-2">Créer un potin</a>
           
          </p>
        </div>
 </section>
 <br>
<div class="album py-5 bg-light">
        <div class="container">
<section class="jumbotron text-center">
 <h3>Potins déja créés</h3>
 <br>
 <% @gossip.each do |user| %>
<div class="row">
    <div class="col-md-4">
    <div class="card mb-3 box-shadow">        
       <div class="card-body">
          <h6 class="card-title"><%= user.id %></h6>
            <p class="card-text"><%= user.first_name %></p>
        <div class="d-flex justify-content-between align-items-center">
    
           <div class="btn-group" >
              <button><a type="button" href="<%= gossip_path(user.id) %>" class="btn btn-sm btn-success">voir</a></button>
              <button><a type="button" href="<%= edit_gossip_path(user.id) %>" class="btn btn-sm btn-primary">editer</a></button>
              <button><a type="button" href="<%= salut_path(user.id) %>" class="btn btn-sm btn-danger" data-confirm="etes vous sur" data-methode="DELETE">suprimer</a></button>
           </div>
       </div>
       </div>
   </div>
   </div>
</div>
<br>
<% end %>
 </section>